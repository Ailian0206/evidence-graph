---
description: PR 创建或更新后触发独立 Claude Code 审核，处理评论后自动合并
alwaysApply: true
---

# 独立 Claude PR 审核

模块完整门禁通过并创建一个 Draft PR 后，运行：

```bash
claude --permission-mode auto --model sonnet -p "/codex-independent-pr-review <PR编号>"
```

- Claude Code 只审核和评论，不修改代码、不提交、不推送、不合并。
- 有 finding 时，Codex 在原分支验证并修复，测试后直接提交、push，再运行一次 Claude 审核。
- Claude 对当前 head 返回 `pass` 且 CI 通过时，Codex 直接使用 merge commit 合并，不等待人工审核。
- Cursor Bugbot 可用时只做附加审核；Autofix 活跃时 Codex 不抢修同一个问题。
